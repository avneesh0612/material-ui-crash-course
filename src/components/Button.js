import Button from "@material-ui/core/Button";
import "./Button.css";

function MuiButton() {
  return (
    <div>
      <Button className="button">Material UI button</Button>
    </div>
  );
}

export default MuiButton;
